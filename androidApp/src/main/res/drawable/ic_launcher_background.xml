<?xml version="1.0" encoding="utf-8"?>
<!-- Diagonal brand-indigo gradient instead of a flat fill, for a bit of depth behind the
     house mark (adaptive icon background layer). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="#6B85FF"
        android:endColor="#28359C" />
</shape>
